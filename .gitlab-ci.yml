image: node:latest

cache:
  paths:
    - app/node_modules/
    # - strapi/node_modules/

stages:
  - test

lint-job:
  stage: test
  script:
    - cd app
    - npm install --only=dev
    - npm run lint